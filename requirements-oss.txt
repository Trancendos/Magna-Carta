# Optional OSS security stack — 100% free, no SaaS (Aikido-free alternative)
# Install: ./scripts/install_zero_cost_security_stack.sh
# Run: ./scripts/run_oss_security_scans.sh
bandit>=1.7
semgrep>=1.60
pip-audit>=2.7
# Transitive override: semgrep pins pyjwt~=2.12; 2.13+ fixes PYSEC-2026-175–179
pyjwt>=2.13.0
# RESOLVED 2026-09-11 — mcp (transitive via semgrep), PYSEC-2026-3481/-3482/-3483.
#
# This block used to record an accepted finding: semgrep hard-pinned
# `mcp==1.23.3`, forcing `mcp>=1.28.1` resolved to 2.0.0 and broke semgrep, so
# the three advisories were accepted and pip-audit was expected to keep
# reporting them. It said "Revisit when semgrep relaxes its pin."
#
# semgrep did not relax the pin — it MOVED it, which is better, and nothing
# here noticed. Re-measured 2026-09-11:
#
#   semgrep 1.173.0 / 1.175.0 / 1.177.0 -> Requires-Dist: mcp==1.29.0
#   mcp 1.23.3 -> 6 OSV advisories
#   mcp 1.29.0 -> 0 OSV advisories
#
# `semgrep>=1.60` above is a floating lower bound, so a fresh resolve already
# takes the current release and therefore already takes mcp 1.29.0. The finding
# is gone, and no override is needed to close it — which is why nothing here
# had to change for it to stop being true, and why nothing reported that it had.
#
# The note is rewritten rather than deleted: an accepted finding reads exactly
# the same whether it is live or stale, and the only thing that distinguishes
# them is someone re-measuring. If pip-audit reports an mcp advisory again,
# re-measure before re-accepting rather than restoring this text.

# NOT PINNED 2026-09-14 — wcmatch, AIKIDO-2026-334325 (ReDoS in extended-glob
# parsing), reported HIGH against 8.5.2 and autofixed to `wcmatch==11.0` by PR #51.
#
# That pin was merged and is reverted here: it cannot resolve. semgrep declares
# `Requires-Dist: wcmatch~=8.3` (measured against 1.173.0 and 1.177.0, the current
# release), which is `>=8.3,<9`, and 8.5.2 is the last 8.x wcmatch ever published.
# So no specifier satisfies both semgrep and the proposed fix, and with the pin in
# place `pip install -r requirements-oss.txt` fails outright:
#
#   ResolutionImpossible: Cannot install -r requirements-oss.txt (line 5)
#   [semgrep>=1.60] ... and wcmatch==11.0
#
# The whole OSS security tier installs from this file, so the pin did not harden
# the scanners — it uninstalled them.
#
# On the advisory itself: OSV returns ZERO records for wcmatch at any version,
# checked with a control in the same run (mcp 1.23.3 -> 6 records), so this is a
# vendor-proprietary finding that cannot be verified here and that pip-audit, which
# reads OSV, will never report either way. wcmatch reaches this file only as a
# semgrep transitive; nothing in this repository imports it, and the patterns
# semgrep feeds it are its own rule files, not untrusted input.
#
# Left unpinned deliberately: semgrep resolves its own wcmatch, and adding a
# constraint here can only break that resolve. Revisit if semgrep widens the pin
# past 9.0, or if a fixed 8.x appears.
